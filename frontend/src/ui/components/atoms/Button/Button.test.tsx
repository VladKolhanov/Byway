describe('Button', () => {
  test.todo('Should correctly render component with label');

  test.todo('Should correctly render \'a\' element if provided variant="link"');

  test.todo(
    'Should render component with default classes if arguments not provided',
  );

  test.todo('Should render component with default classes and className');

  test.todo(
    'Should render component with custom classes if arguments provided',
  );

  test.todo('Should render component with props');
});
